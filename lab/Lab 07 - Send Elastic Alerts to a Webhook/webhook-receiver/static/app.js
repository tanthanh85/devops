const events=document.getElementById("events"),empty=document.getElementById("empty");
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
async function refresh(){try{const r=await fetch("/api/events",{cache:"no-store"}),d=await r.json();document.getElementById("health").textContent="Online";document.getElementById("total").textContent=d.items.length;document.getElementById("active").textContent=d.items.filter(x=>x.status==="active").length;document.getElementById("recovered").textContent=d.items.filter(x=>x.status==="recovered").length;empty.hidden=d.items.length>0;events.innerHTML=d.items.map(x=>`<article class="event ${esc(x.status)}"><div><span class="severity">${esc(x.severity)}</span><b>${esc(x.rule_name)}</b></div><p>${esc(x.reason)}</p><small>${esc(x.received_at)} · ${esc(x.group)} · value ${esc(x.value)} / threshold ${esc(x.threshold)}</small></article>`).join("")}catch(e){document.getElementById("health").textContent="Unavailable"}}refresh();setInterval(refresh,5000);

